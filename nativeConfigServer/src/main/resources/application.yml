server:
  port: 8016
spring:
  application:
    name: nativeConfigServer
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared